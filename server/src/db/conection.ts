import mysql from 'mysql';
import { database } from './keys' // keys hace referencia a 'database' 
                           // de ese archivo.

// const conection = mysql.createConnection(database);

// export default conection;